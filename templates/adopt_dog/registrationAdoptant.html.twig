{% extends 'base.html.twig' %}

{% block body %}


    <div class="container col-9">
        {{ form_start(formAdoptant) }}
        {{ form_widget(formAdoptant) }}
        <input class="btn btn-secondary" type="submit" value="Enregistrer">
        {{ form_end(formAdoptant) }}
    </div>


{% endblock %}